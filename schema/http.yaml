protocol: http
includes:
  - common
  - tls

fields:
  password:
    type: string
  username:
    type: string

targets:
  clash:
    template:
      username:
        from: username
      password:
        from: password
  surge:
    type: "http"
    template:
      username:
        from: username
      password:
        from: password
